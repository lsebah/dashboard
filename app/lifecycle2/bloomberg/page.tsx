import BloombergPage from '@/app/lifecycle/bloomberg/page'

// Réutilise intégralement la vue « Niveaux à extraire (Bloomberg) » du Lifecycle.
export default function Bloomberg2Page() {
  return <BloombergPage />
}
